apiVersion: 1

datasources:
  - name: The Pool Prometheus
    uid: the-pool-prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: 15s
